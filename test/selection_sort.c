#include <cs50.h>
#include <stdio.h>

void sort_array(int unsorted_array[]);

int main()
{
}

void sort_array(int unsorted_array[])
{
    int size = sizeof unsorted_array / sizeof unsorted_array[0];
}
