#include <cs50.h>
#include <stdio.h>

bool validateCreditCard();

int main(void)
{

}

bool validateCreditCard()
{
    long credit = get_long("Enter your credit card nr: ");
    sum = 0;
    while (credit > 0) {
        int digit = value % 10;
        digit = digit * 2;

        if (digit > 9){
            digit 
        }

        value /= 10;
    }

}